# Tasks

- [x] Task 1: 修改 `AGENTS.md` 的 `/plan 前置规则` 段落，追加 plan 确认后调用 `/spec` 的流程说明
  - 在"执行流程"第 4 步后追加：`5. 用户确认 plan 后 → 调用 /spec 进入 Spec 模式，编写 spec.md / tasks.md / checklist.md`
  - 在"执行流程"中注明跳过规则：用户可说"不用 spec"时跳过

# Task Dependencies

无（单任务）
