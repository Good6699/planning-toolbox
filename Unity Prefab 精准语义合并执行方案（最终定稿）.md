# Unity Prefab 精准语义合并执行方案（最终定稿）

## 1. 方案简介

本方案摒弃传统 SVN 纯文本行合并、全文件覆盖合并逻辑，针对 Unity **.prefab / .unity** 文件实现**结构化语义精准合并**。通过对比两个版本的 YAML 结构化差异，识别"无冲突兼容修改"与"语义互斥冲突"，保留本地无关修改的同时精准叠加源端变更，冲突统一强制采用**源版本覆盖本地**策略。

核心优势：**保留本地无关修改、精准叠加源端变更、冲突自动裁决、无需还原本地工程、无人工干预**，彻底解决 Unity 预制体跨版本合并错乱、丢节点、丢属性、引用断裂问题。

## 2. 前置依赖条件

- 工程资源序列化模式设置为 **Force Text**（YAML 文本格式）

- 源文件与目标文件 **meta 文件一致、GUID 一致、仓库相对路径一致**

- 仅针对 .prefab、.unity 场景文件启用语义合并，代码类文件沿用普通合并逻辑

## 3. 方案核心架构

### 3.1 三方合并模型（Base / Local / Remote）

本方案采用标准三方合并模型，而非简单的"源端增量叠加"：

| 角色 | 含义 | 来源 |
|------|------|------|
| **Base** | 共同祖先版本 | 本地与源端最后一次分叉前的 .prefab |
| **Local** | 本地当前版本 | 工作副本中的 .prefab |
| **Remote** | 源端目标版本 | 筛选区间内源端最终的 .prefab |

通过 Base 定位分叉点，才能正确判断"Local 改了什么"和"Remote 改了什么"，进而判定语义冲突。

### 3.2 单版本对合并 vs 多版本增量叠加

本方案支持两种工作模式，根据筛选的版本数量自动选择：

| 模式 | 适用场景 | 做法 |
|------|----------|------|
| **单版本对合并** | 只选了一个源端版本 | Base = 本地当前 SVN 基线，Remote = 所选版本，Local = 工作副本 |
| **多版本增量叠加** | 选了连续版本区间 | Base = 区间起始版本的前一版本，Remote = 区间最终版本。中间版本产生的语义操作指令逐条叠加到 Remote 上后，再做三方合并 |

> **注**：多版本模式仅对 Remote 一端叠加中间增量，Local 端不叠加，因此不会出现 N 个版本做 N 次完整三方合并的性能问题。只需一次 YAML diff（对比 Base → Remote），外加一次轻量级指令叠加（对比 Remote 与中间版本的属性级差异）。

### 3.3 结构化解析方式

**不依赖 UnityYAMLMerge 做语义提取**。UnityYAMLMerge 的作用仅限于合并完成后的 **YAML 结构合法性验证**，不参与核心的 diff 和合并逻辑。核心解析和合并由自定义引擎完成：

```
自定义 YAML 解析器（按 --- !u! 分段提取）
  ↓
自定义结构化 Diff（按 fileID 匹配节点，按属性路径匹配值）
  ↓
自定义合并裁决器（无冲突保留双方，冲突选 Remote）
  ↓
合并结果输出 .prefab
  ↓
UnityYAMLMerge --force 验证 YAML 结构合法性（可选）
```

## 4. 可识别的精准语义变更

### 4.1 节点层级变更

- 新增节点、删除节点

- 节点重命名、父子层级调整、同级节点排序变动

### 4.2 组件状态变更

- 新增组件、移除组件

- 组件启用/禁用状态切换

### 4.3 实体属性变更

- Transform：坐标、旋转、缩放修改

- UI 文本：内容、字号、字体颜色、对齐方式、行间距

- 图像资源：Sprite、材质、贴图引用替换

- 布局参数：宽高、边距、居中、适配参数修改

### 4.4 文件版本变更

- 文件新增、文件删除、迭代内容修改

## 5. 版本语义解析规则

系统不做全文本对比，采用**结构化定点差分**机制：

1. 以 Base 版本为锚点，分别对比 Base → Local（本地修改）和 Base → Remote（源端修改）

2. 自动过滤 Unity 自动生成的冗余字段、默认填充字段（如 `m_EditorHideFlags`、`m_EditorClassIdentifier`），只保留**人工有效变更**

3. 每一次变更标准化为 **语义操作指令**，结构包含：节点 fileID、组件类型、操作类型（add/del/mod）、属性路径、旧值、新值

4. 自动去重：对 Remote 端多个中间版本产生的相同指令做合并去重（相同 fileID + 相同属性路径 → 以最新版本为准）

## 6. 精准合并核心执行逻辑

### 6.1 无冲突合并逻辑（核心亮点）

Local 修改与 Remote 修改**不作用于同一个节点同属性**时，双向完全保留：

- Remote 新增节点 → Local 同步新增，Local 其他修改不受影响

- Remote 删除节点 → Local 同步删除对应 fileID 的节点

- Remote 修改某属性、Local 修改无关属性 → **两边修改全部保留**

实现效果：**本地有效修改不丢失，源端增量变更完整叠加**。

### 6.2 语义冲突判定标准

满足以下任一情况，判定为**语义互斥冲突**：

- 同一节点的**同一属性**，Local、Remote 均发生修改（相对 Base 而言）

- 同一节点，一端删除、另一端修改属性/层级

- 同一组件，两端同时修改状态、参数、配置

### 6.3 冲突统一处理策略

**所有语义冲突，无条件优先使用 Remote 版本覆盖 Local 修改**

- 属性冲突：丢弃本地修改，强制写入 Remote 最新属性值

- 节点冲突：以 Remote 节点状态为准（增/删/层级）

- 组件冲突：以 Remote 组件配置、启用状态为准

- 全程无弹窗、无人工选择、不中断合并流程

## 7. 最终合并结果公式

**最终文件 = Local 所有无冲突修改 + Remote 全部增量修改（冲突部分 Remote 全覆盖）**

区别于全量覆盖：不会清空本地所有改动，仅覆盖真正冲突的部分，最大化保留本地开发内容。

## 8. 能力边界界定

### 8.1 完全稳定支持场景

- 同 Unity 大版本内 Prefab/场景常规 UI、节点、组件、属性迭代修改

- 多版本连续增量变更精准重放合并

- 本地存在大量无关修改，可安全并行合并，互不干扰

### 8.2 有限支持场景（需额外注意）

- **嵌套 Prefab（Nested Prefab）**：能识别引用关系的 fileID，不展开嵌套结构内部做递归合并。嵌套 Prefab 作为整体处理，而非拆解合并

- **Prefab Variant（变体）**：能识别 `m_Prefab` 父引用，但对变体覆盖层的属性变更做逐层合并时，Base 版本的选择需要精确到变体基类的分叉点

### 8.3 无法完美处理的场景（边界限制）

- 跨 Unity 主版本升级导致的底层序列化结构自动变更

- 自定义脚本字段重命名、删除、结构体逻辑重构（因为字段名变了，Base/Local/Remote 三者之间无法建立正确的属性路径映射）

- 大规模图集、资源路径全局重构类变更

- 复杂嵌套变体预制体的多层级差异化重构

- **fileID 漂移**：如果 Prefab 被重建（如从fbx重新生成、用脚本批量重设），fileID 可能全部变化，此时按 fileID 匹配节点的策略会完全失效。需降级为按 `m_Name` + 层级路径匹配

## 9. 与传统合并的核心差异

| 合并方式 | 本地无冲突修改 | 冲突处理 | Prefab 安全性 |
|---|---|---|---|
| SVN 文本行合并 | 大概率错乱丢失 | 行级冲突、极易炸文件 | 极低，易坏结构 |
| 全文件覆盖合并 | 全部丢失 | 强制覆盖全部 | 中等，但丢失本地改动 |
| 本文语义精准合并 | **完整保留** | **冲突 Remote 优先** | 最高，结构化安全合并 |

## 10. 待验证项（PoC 阶段需确认）

1. YAML 解析器能否正确处理真实项目中的各种 .prefab 格式变体

2. fileID 在常规 SVN 迭代中是否足够稳定，可以作为节点匹配主键

3. 多版本模式下，中间版本的增量指令叠加到 Remote 是否会产生语义冲突（两个中间版本改了同一处）

4. 大型 .prefab（>1MB）的解析 + diff 性能是否在可接受范围内

> （注：文档部分内容由 AI 生成）
