# SVN一键对比 GUI 工具创建

**时间**：2026-04-19 10:54

## 任务目标

优化 SVN一键对比.bat 的操作界面，改为 GUI 版本：
1. SVN URL 改下拉选择，可新增并持久化
2. 日期改成下拉选择（年/月/日），默认当年1月1日~今天
3. 输出路径弹窗选择，默认输出到 `./输出/` 子文件夹

## 实现方案

使用 Python tkinter 创建 GUI，替代原批处理文件的命令行交互。

### 核心文件

| 文件 | 说明 |
|------|------|
| `svn_compare_gui.py` | Python tkinter GUI 主程序（18KB） |
| `SVN一键对比GUI.bat` | 启动器，调用 GUI |
| `svn_gui_config.json` | 配置文件，存储 SVN URL 列表和并发数设置 |
| `输出/` | 默认输出文件夹 |

### 功能实现

1. **SVN URL 下拉选择**
   - Combobox 控件，支持选择已有 URL 或手动输入新 URL
   - 「＋ 添加」按钮：将当前输入保存到列表并持久化到配置文件
   - 「－ 删除」按钮：从列表中移除当前选中项
   - 预置 URL：`http://192.168.1.41:8080/svn/D3/branches/20240606_KR2/gameData/Text/Texts.xlsm`

2. **日期下拉选择**
   - 年：2020 ~ 当前年份，默认当前年
   - 月：01-12，默认开始=01，结束=当前月
   - 日：01-31，默认开始=01，结束=当前日

3. **输出路径**
   - Entry 显示当前路径
   - 「浏览…」按钮调用 `filedialog.asksaveasfilename`
   - 默认路径：`./输出/svn_compare_YYYYMMDD.xlsx`

4. **高级选项（折叠）**
   - 下载并发数（默认 6）
   - 解析并发数（默认 8）

5. **日志区**
   - 深色终端风格
   - 彩色输出：info(灰)、ok(绿)、warn(黄)、error(红)

### 调用关系

```
SVN一键对比GUI.bat
    ↓
svn_compare_gui.py (GUI 界面)
    ↓ 点击「开始执行」
svn_oneclick_compare.py (核心逻辑)
```

## 使用方法

双击 `SVN一键对比GUI.bat` 启动图形界面，填写参数后点击「▶ 开始执行」。

## 验证结果

- GUI 脚本可正常导入（无语法错误）
- 配置文件读写正常
- 输出文件夹已创建

## 备注

原 `SVN一键对比.bat` 保留，用户可选择命令行或 GUI 版本。
