APP_VERSION = "v1.1.60"
# ⚠ 更新服务器地址必须指向【运行更新服务器的那台机器的固定 IP】
# 不要用 socket.gethostbyname 动态获取：它在客户端运行时返回的是【客户端自己的 IP】，
# 导致只有开发机本机能收到更新，其他人全部连自己机器。
# 若更新服务器所在机器的 IP 变了，改这里并重新打包分发。
UPDATE_URL = "http://192.168.11.15:8080/"
