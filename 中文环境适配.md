# 中文 Windows 环境适配记录

## 1. SVN 中文输出解析

### 问题
中文版 SVN 的 `svn info` 输出字段为中文（如"工作副本根目录"），代码按英文关键词"Working Copy Root Path"解析永远匹配不到。

### 修复
改用 `svn info --show-item wc-root` 直接输出路径值，不依赖语言。

```python
# ❌ 依赖语言
r = subprocess.run(["svn", "info", path], ...)
for line in r.stdout.splitlines():
    if line.startswith("Working Copy Root Path: "):  # 英文版才有
        wc_root = line[23:]

# ✅ 不依赖语言（SVN 1.8+）
r = subprocess.run(["svn", "info", "--show-item", "wc-root", path], ...)
wc_root = r.stdout.strip()
```

`--show-item` 支持的参数：`wc-root`, `url`, `revision`, `last-changed-revision`, `last-changed-author`, `last-changed-date` 等。这是 SVN 1.8 引入的特性，**中文/英文/日文/韩文版均可用**。

### 涉及文件
- svn_compare_gui.py：`_wf_execute_one()`、`_check_svn_and_confirm()`

---

## 2. subprocess 编码统一处理

### 问题
SVN 输出可能含中文，在某些 Windows 环境中默认编码（GBK/cp936）可能导致 UnicodeDecodeError。

### 修复
所有 `subprocess.run`/`Popen` 统一传参：

```python
encoding="utf-8", errors="replace"
```

### 处理策略
- 优先 UTF-8 解码
- 如果 UTF-8 不匹配就用 `errors="replace"` 以 `�` 替换不可解码字符，防止崩溃
- 如需更强兼容性，可尝试 `['utf-8', 'gbk', 'cp936', 'latin-1']` 逐一尝试（见 svn_oneclick_compare.py 的 _svn 函数）

### 涉及文件
所有调用 subprocess 的文件。

---

## 3. Windows 批处理编码陷阱

### 问题
Windows 的 `cmd.exe` 默认使用 **GBK（代码页 936）** 编码解释批处理文件：

| 文件编码 | 现象 |
|---------|------|
| UTF-8 无 BOM | `cmd.exe` 将 UTF-8 字节误识别为 GBK → 中文乱码，甚至吃掉有效字符（如 set 变为 et）|
| UTF-8 with BOM | 部分 Windows 10/11 版本正常，但老旧系统不支持 |
| **GBK** | **✅ 始终正常显示中文** |

### 规则
- 凡含中文的 `.bat` / `.cmd` 文件，**必须保存为 GBK（ANSI）编码**
- 不要写 `chcp 65001`（切 UTF-8 代码页），部分版本 CMD 会崩溃或输出乱码
- 英文专用批处理可随意

### 涉及文件
- SVN查找上一版本.bat
- 其他含中文的批处理文件

---

## 4. SVN URL 中文编码

### 问题
`svn info` 返回的 URL 中中文部分被 URL 编码（如 `D3_%E5%9B%BE%E6%96%87%E6%95%99%E7%A8%8B.xlsm`），直接显示给用户不可读。

### 修复
```python
from urllib.parse import unquote
svn_url = unquote(svn_url)
# http://.../D3_%E5%9B%BE... → http://.../D3_图文教程.xlsm
```

### 涉及文件
svn_compare_gui.py：`_on_file_drop()` 中解析 SVN URL 时。

---

## 5. 中文日期格式兼容

### 问题
中文 Windows 下 `%date%` 输出格式为 `周四 2026/04/16`，而英文版为 `Thu 04/16/2026`。直接解析 `%date%` 提取年月日不可靠。

### 修复
用 PowerShell 获取标准 ISO 格式日期：

```bat
for /f %%i in ('powershell -Command "Get-Date -Format yyyy-MM-dd"') do set TODAY=%%i
```

### 涉及文件
svn_query.py 的启动脚本/参数处理。

---

## 6. VBS 静默启动 + 中文路径

### 问题
- 用 `pythonw.exe` 启动 GUI 会有一个短暂的 CMD 窗口闪现
- 中文用户名/路径含空格时启动命令可能失败

### 修复
VBS 启动器使用 `Chr(34)` 包裹路径，`shell.Run ..., 0, False` 静默启动：

```vbs
strPython = "C:\Users\admin\AppData\Local\Programs\Python\Python313\python.exe"
cmdLine = Chr(34) & strPython & Chr(34) & " " & Chr(34) & pyScript & Chr(34)
shell.Run cmdLine, 0, False
```

- `0` → 隐藏窗口
- `False` → 异步执行
- `Chr(34)` → 双引号，保护含空格的中文路径

### 涉及文件
SVN一键对比GUI.vbs

---

## 7. Python stdout/stderr 编码适配

### 问题
GUI 模式下（无控制台），Python 的 `sys.stdout.encoding` 可能是 `None`，print 会崩溃。

### 修复
```python
import sys
sys.stdout.reconfigure(encoding='utf-8', errors='replace')
sys.stderr.reconfigure(encoding='utf-8', errors='replace')
```

或用 `try/except` 包裹：
```python
try:
    sys.stdout.reconfigure(encoding='utf-8', errors='replace')
except Exception:
    pass
```

### 涉及文件
svn_compare_gui.py（文件顶部）

---

## 8. UI 字体选用

### 原则
| 用途 | 字体 | 原因 |
|------|------|------|
| 标签/按钮/输入框 | **微软雅黑** | 中文 Windows 默认美观中文字体 |
| 代码/路径/日志 | **Consolas** | 等宽字体，路径对齐清晰 |
| 数据/表格 | **微软雅黑**（或 Consolas） | 美观优先 |

所有字体大小统一用 `9` 号，标题用 `10` 或 `14` 加粗。

### 涉及文件
svn_compare_gui.py 全部 UI 构建方法。

---

## 9. 总结：一段代码同时兼容中英文 SVN

```python
def get_svn_info(path):
    """获取SVN工作副本根路径（兼容中文/英文/韩文版SVN）"""
    r = subprocess.run(
        ["svn", "info", "--show-item", "wc-root", path],
        capture_output=True, text=True,
        encoding="utf-8", errors="replace",  # 编码容错
        timeout=15,
        creationflags=subprocess.CREATE_NO_WINDOW  # 隐藏CMD窗口
    )
    if r.returncode != 0:
        return None
    return r.stdout.strip()
```
