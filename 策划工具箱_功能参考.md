# 策划工具箱 功能参考

与 AI 沟通功能需求时，使用 `页签 > 功能 > 子功能` 格式。

---

## 用法速记

```
svn > 摘要模式        → SVN记录页签切换为修改摘要模式
svn > 对比模式        → SVN记录页签切换为对比Excel模式
svn > 导出模式        → SVN记录页签切换为导出文件模式
merge > 筛选查询       → 语义合并配置双地址后筛选版本
merge > 语义分析       → 对版本文件做AI语义分析
merge > 开始合并       → 执行文件级精准合并
upload > 上传SVN       → 复制文件到SVN目录并上传
wf > 工作流.0         → 执行第一个工作流
tr > 翻译              → 调用AI翻译文本
revert_svn > 回退      → 回退指定路径下所有本地修改
```

---

## 一、SVN 记录 `svn`

| 功能 | 说明 |
|------|------|
| 地址输入 | 输入 SVN URL，支持历史记录下拉建议 |
| 浏览本地副本 | 浏览按钮选择本地 SVN 工作副本目录 |
| 打开文件夹 | 打开当前 URL 对应的本地文件夹 |
| 修改摘要模式 | 查询版本历史，统计谁改了哪些文件，输出摘要 |
| 对比Excel模式 | 对比两个版本之间的 Excel 文件差异，生成对比结果 |
| 导出文件模式 | 从 SVN 仓库导出指定版本的文件 |
| 日期范围筛选 | 设置起始/结束日期，限制查询范围 |
| 今日快捷按钮 | 一键设置日期范围为今天 |
| 关键词过滤 | 按提交备注关键词过滤版本 |
| 作者过滤 | 按 SVN 提交者过滤版本 |
| 输出目录 | 指定结果输出路径 |
| 高级设置 | 弹窗配置 SVN 用户名/密码、排除目录、文件名过滤 |
| 执行 | 开始 SVN 查询/对比/导出任务 |
| 日志 | 流式输出实时执行日志 |
| 清除缓存 | 清空离线缓存的对比结果 |
| 高级 > 保存预设 | 保存当前高级设置为一组预设 |
| 高级 > 删除预设 | 删除已保存的预设 |

### 执行流程

```
配置地址 → 选择模式 → 筛选条件 → 点击执行 → SSE流式日志
```

---

## 二、语义合并 `merge`

| 功能 | 说明 |
|------|------|
| 源SVN地址 | 待合并的原始版本仓库 URL |
| 目标路径 | 最终合并落地的本地工作副本路径 |
| 起始日期 | 筛选时间段起始 |
| 结束日期 | 筛选时间段结束 |
| 提交者过滤 | 按 SVN 提交者筛选 |
| 备注关键词 | 按提交备注模糊匹配（逗号分隔多个） |
| 筛选查询 | 拉取符合条件的版本列表 |
| 版本列表 | 展示版本号、提交时间、提交人、备注 |
| 版本全选/反选/清空 | 快速操作版本复选框 |
| 变更文件 | 选中的版本中变更的文件列表 |
| 文件全选/反选/清空 | 快速操作文件复选框 |
| 路径筛选 | ⚙ 弹出窗口设置包含/排除路径 |
| 语义分析 | 对勾选的文件做 AI 语义分析（需配置 API） |
| 开始合并 | 执行文件级精准 SVN 合并 |
| 日志 | 流式输出合并日志 |

### 核心规则

- 只合并用户手动勾选的文件
- 存在本地冲突时自动使用源版本覆盖（`theirs-full`）
- 无需还原本地项目、无需清空本地修改
- 合并完成后唤起 TortoiseSVN 提交弹窗

---

## 三、复制合并 `upload`

| 功能 | 说明 |
|------|------|
| 源目录 | 要复制的文件来源目录 |
| 浏览源目录 | 选择本地源目录 |
| 刷新文件列表 | 重新扫描源目录文件 |
| 文件列表 | 源目录下的所有文件和子目录 |
| 全选/取消全选 | 批量操作文件复选框 |
| 目标目录 | SVN 工作副本目标路径 |
| 浏览目标目录 | 选择 SVN 工作副本目录 |
| 上传到 SVN | 复制选中的文件到目标目录 → svn add → svn changelist 分组 → 打开 TortoiseSVN 提交弹窗 |
| 日志 | 流式输出上传日志 |
| 清理changelist | 清空 SVN changelist 标签 |

### 执行流程

```
配置源目录 → 勾选文件 → 配置目标SVN目录
→ 点击上传 → 复制文件 → svn add → 打changelist标签 → 打开TortoiseSVN提交
```

---

## 四、工作流 `wf`

| 功能 | 说明 |
|------|------|
| 新建工作流 | 创建空白工作流 |
| 工作流列表 | 所有已创建的工作流 |
| 工作流名称 | 点击进入内联编辑 |
| 工作流复选框 | 全选/取消所有步骤 |
| 展开/折叠 | ▶ 箭头折叠或展开步骤列表 |
| 播放工作流 | ▶ 执行本工作流所有勾选的步骤 |
| 复制工作流 | 📋 复制整个工作流及其步骤 |
| 删除工作流 | ✕ 删除工作流及其所有步骤 |
| 添加步骤 | + 添加步骤 → 弹出类型选择菜单 |
| 步骤复选框 | 勾选要执行的步骤 |
| 步骤设置 | ⚙ 打开步骤配置弹窗 |
| 删除步骤 | ✕ 删除单个步骤 |
| 日志 | 流式输出执行日志 |

### 步骤类型

| 步骤类型 | 标识 | 功能 |
|----------|------|------|
| **导出文字表** | `export_text` | 从 Excel 导出指定语言的文字表 |
| **上传SVN** | `upload_svn` | 上传指定目录的文件到 SVN |
| **合并表格** | `merge_table` | 合并多个 Excel 文件为一个 |
| **合并翻译** | `merge_translation` | 将翻译结果合并回原文件 |
| **导出错误码** | `export_error_code` | 导出错误码映射表 |
| **锁定SVN** | `lock_svn` | 对指定文件加锁，防止他人修改 |
| **解锁SVN** | `unlock_svn` | 对指定文件解锁 |
| **打开表格** | `open_tables` | 用 Excel 打开一个或多个文件 |
| **SVN回退** | `revert_svn` | 回退指定路径下所有本地修改 |

### 步骤类型详情

#### export_text（导出文字表）

| 字段 | 类型 | 说明 |
|------|------|------|
| 主文件路径 | string | Excel 源文件路径 |
| 工具目录 | array | comma text export 工具目录 |
| 语言列表 | string | 逗号分隔的语言代码 |

#### upload_svn（上传SVN）

| 字段 | 类型 | 说明 |
|------|------|------|
| 源目录 | array | 要上传的本地目录（可多个） |

#### merge_table（合并表格）

| 字段 | 类型 | 说明 |
|------|------|------|
| 输入文件 | array | 要合并的 Excel 文件 |
| 输出目录 | string | 合并结果存放目录 |
| 合并前缀 | string | 输出文件名前缀 |
| 标题行 | string | 标题行号（如 1） |
| ID列 | string | ID 列号（如 1） |

#### merge_translation（合并翻译）

| 字段 | 类型 | 说明 |
|------|------|------|
| 翻译文件 | string | 翻译后的 Excel |
| 原始文件 | string | 原始 Excel |
| Sheet名称 | string | 要处理的工作表名 |

#### export_error_code（导出错误码）

| 字段 | 类型 | 说明 |
|------|------|------|
| 根目录 | string | 项目根目录 |
| 语言代码 | string | 逗号分隔（如 JA_JP,KO_KR） |

#### lock_svn（锁定SVN）

| 字段 | 类型 | 说明 |
|------|------|------|
| 目标文件路径 | string | 要锁定的文件 |
| 更新目录 | array | 锁定前先 update 的目录 |
| 锁定消息 | string | 锁定备注 |

#### unlock_svn（解锁SVN）

| 字段 | 类型 | 说明 |
|------|------|------|
| 目标文件路径 | string | 要解锁的文件 |
| 更新目录 | array | 解锁前先 update 的目录 |
| 解锁消息 | string | 解锁备注 |

#### open_tables（打开表格）

| 字段 | 类型 | 说明 |
|------|------|------|
| 文件路径 | array | 逗号分隔的 Excel 路径 |

#### revert_svn（SVN回退）

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 回退路径 | string | — | 要回退的本地工作副本目录 |
| 排除路径 | array | `[]` | 不回退的文件/文件夹（逗号分隔，支持相对路径或文件名匹配） |
| 删除未版本文件 | bool | false | 复选框控制，勾选后永久删除未版本控制文件（不进回收站） |

**执行流程：**

```
更新到最新版本（svn update --accept theirs-full，遇锁自动 cleanup）
→ 扫描本地修改（svn status）
→ 过滤排除路径
→ 批量回退（svn revert --targets）
→ 检查冲突（svn cat 拉取 HEAD + resolve --accept working 覆盖）
→ 可选：删除未版本控制文件
```

---

## 五、翻译 `tr`

| 功能 | 说明 |
|------|------|
| 翻译文件路径 | 要翻译的 Excel 文件 |
| 参考文件 | 已有的翻译参考文件 |
| 源语言 | 源语言列标识 |
| 目标语言 | 勾选要翻译的目标语言（多选） |
| 语言ID高级设置 | ⚙ 弹窗管理语言名称 → 匹配ID 映射表 |
| API URL | AI 翻译 API 地址 |
| API Key | AI 翻译 API 密钥 |
| 模型 | AI 模型名称 |
| 输出目录 | 翻译结果存放目录 |
| 翻译Prompt | 自定义翻译提示词 |
| 批处理量 | 每次发送的翻译批次数 |
| 开始翻译 | 执行 AI 翻译任务 |
| 日志 | 流式输出翻译日志 |

---

## 六、通用组件

| 组件 | 说明 |
|------|------|
| 状态栏 | 顶栏右侧 [● 系统空闲]，任务执行时变为 [● 运行中] |
| 页签导航栏 | 左侧固定导航：SVN记录、语义合并、复制合并、工作流、翻译；运行时显示黄点 |
| 关闭按钮 | ✕ 关闭到系统托盘（桌面版） |
| 日志 | 所有页签统一 320px 固定高度日志区，流式 SSE 实时输出 |
| 目录拖放 | 输入框支持从文件管理器拖入路径 |
| 自定义日期选择器 | 替代原生 date picker，深色主题风格 |
| 浏览对话框 | 通过 pywebview 调用系统文件/目录选择对话框 |

---

## 七、交流示例

```
你: svn > 对比模式 > 结果不对，版本号范围有问题
我: 检查日期过滤逻辑和版本号提取

你: merge > 合并 > 冲突文件被跳过了
我: 检查 merge --accept theirs-full 执行结果

你: upload > 上传SVN > 提交对话框显示太多文件
我: 检查 changelist 过滤逻辑是否只标记了勾选的文件

你: wf > 工作流.0 > 步骤.2 > export_text 弹窗里加个字段
我: 给第一个工作流的第三个步骤设置弹窗加字段

你: revert_svn > 排除不生效
我: 检查 _svn_filter_exclude 匹配逻辑和排除路径格式

你: tr > 翻译 > 目标语言没出现
我: 检查语言映射表配置
```
